# templates/create_key_pair.yml.j2
amazon.aws.ec2_key:
  name: "{{ key_name }}"
  region: "{{ region }}"
  aws_access_key: "{{ ec2_access_key }}"
  aws_secret_key: "{{ ec2_secret_key }}"
  key_material: "{{ lookup('file', ssh_public_key_file) }}"
